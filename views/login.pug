extends layout

block content
    -data = data || {}
    if errors
        ul
        for error in errors
            li= error.msg
    .div
    .container
        .d-flex.justify-content-center.h-100
            .card
            .card-header
                h3 Sign In
            .card-body
                form(action="/login" method="POST")
                    .input-group.form-group
                        .input-group-prepend
                        span.input-group-text
                            i.fas.fa-user
                        input.form-control(type='text', id="uname", placeholder='username', name="uname", value=data.uname)
                    .input-group.form-group
                        .input-group-prepend
                        span.input-group-text
                            i.fas.fa-key
                        input.form-control(type='password', id="password", name="password", placeholder='password' value=data.password)
                    .input-group.form-group
                        input.button.float-right(type='submit', value='Submit')


//- extends layout

//- block content
//-     -data = data || {}
//-     if errors
//-         ul
//-         for error in errors
//-             li= error.msg

//-     form(action="/login" method="POST")
//-         label(for="username") Username:
//-         input(
//-             type="text"
//-             id="uname"
//-             placeholder="username"
//-             name="uname"
//-             value=data.uname
//-         )

//-         label(for="pw") Password:
//-         input(
//-             type="password"
//-             id="password"
//-             name="password"
//-             value=data.password
//-         )
//-         input(type="submit" value="Submit")