include layout.pug

block content
    if users.length
        table
            tr
                th Name
                th Email
                th Password
            each user in users
                tr
                    td= user.uname
                    td= user.email
                    td= user.password
    else
        p No registrations yet

        